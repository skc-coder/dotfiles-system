Extract the complete text content from the attached PDF(s), verbatim, with zero summarization or paraphrasing. Rules: 1. Output every word exactly as it appears. 2. For PDFs with mixed selectable and image-based (scanned) text, run OCR on the image portions and merge the output in correct reading order with the selectable text. Do not skip image-only pages. 3. Preserve structure as plain text equivalents: Tables: reproduce as Markdown tables or aligned plain-text columns if too complex for Markdown. Equations: reproduce in LaTeX, inline with single dollar signs or block with double dollar signs. Flowcharts and diagrams: describe the shape and flow in structured text, e.g. [Box: Start] -> [Decision: X?] -> Yes: [Box: A] / No: [Box: B], preserving every label exactly as written. 4. Maintain original top-to-bottom, left-to-right reading order, including multi-column layouts, finish left column before right column unless layout clearly indicates otherwise. 5. Insert a clear marker between documents: START OF PDF: filename and END OF PDF: filename. 6. Do not add commentary, summaries, explanations, or section headers that aren't in the original. Output only the extracted content plus the markers. 7. If a portion is illegible or OCR confidence is low, mark it as [ILLEGIBLE] rather than guessing or omitting.
