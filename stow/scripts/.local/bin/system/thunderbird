#!/usr/bin/env bash
# thunderbird - Wrapper script for Thunderbird under Sway

if ! pgrep -x "thunderbird" > /dev/null; then
    /usr/bin/thunderbird "$@" &
else
    swaymsg '[app_id="(?i)thunderbird"] focus' || swaymsg '[class="(?i)thunderbird"] focus' || /usr/bin/thunderbird "$@" &
fi
