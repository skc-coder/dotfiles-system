#!/usr/bin/env bash
# megasync - Wrapper script for MEGAsync under Sway

if ! pgrep -x "megasync" > /dev/null; then
    /usr/bin/megasync "$@" &
else
    swaymsg '[app_id="(?i)megasync"] focus' || swaymsg '[class="(?i)megasync"] focus' || /usr/bin/megasync "$@" &
fi
